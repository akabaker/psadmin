$ = jQuery = require('jquery'); //both $ or jQuery can be used for jquery
var React = require('react');
var Home = require('./components/homePage');

React.render(<Home />, document.getElementById('app'));